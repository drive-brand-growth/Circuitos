# Integrating Claude Agent Memory with GHL + Virtual LPR
## Complete Integration Guide

**Purpose:** Connect Claude Agent Memory API to existing GHL workflows and Virtual LPR system

---

## 🎯 INTEGRATION ARCHITECTURE

### Before (One-Shot Claude API):
```
┌─────────────────────────────────────────────────────────┐
│ GHL Workflow                                             │
├─────────────────────────────────────────────────────────┤
│ 1. New Contact Created (Virtual LPR signal detected)    │
│ 2. Call Claude API (score lead)                         │
│ 3. Update custom fields (lpr_score)                     │
│ 4. IF lpr_score >= 70 → Cold Email Sequence             │
│ 5. IF lpr_score < 70 → Nurture Sequence                 │
└─────────────────────────────────────────────────────────┘
```

**Limitations:**
- ❌ No memory - every lead scored independently
- ❌ No agent collaboration - Copywriter doesn't see Lead Scorer's work
- ❌ Static workflows - same rules for everyone

### After (Memory-Enhanced Multi-Agent):
```
┌─────────────────────────────────────────────────────────┐
│ GHL Workflow                                             │
├─────────────────────────────────────────────────────────┤
│ 1. New Contact Created (Virtual LPR signal detected)    │
│ 2. Call Claude Agent Memory API (Lead Scorer)           │
│    ↓ Agent remembers past interactions                  │
│ 3. Agent decides next action dynamically                │
│    ↓ If score >= 70 AND first interaction               │
│ 4. Call Claude Agent Memory API (Copywriter)            │
│    ↓ Copywriter sees Lead Scorer's analysis             │
│ 5. Send personalized email (generated by agent)         │
│ 6. Track outcome → ML feedback loop                     │
│    ↓ System learns which signals = conversions          │
│ 7. Adjust Virtual LPR weights automatically             │
└─────────────────────────────────────────────────────────┘
```

**Benefits:**
- ✅ Agents remember all past scoring/outreach attempts
- ✅ Multi-agent collaboration (shared context)
- ✅ Dynamic workflow decisions based on conversation history
- ✅ Self-improving Virtual LPR weights

---

## 📋 STEP-BY-STEP INTEGRATION

### Step 1: Deploy Claude Agent Memory API (30 min)

**1.1 Deploy to Vercel**
```bash
cd /home/user/Circuitos/api
npm install
npx vercel --prod

# Add environment variables
npx vercel env add CLAUDE_API_KEY production
npx vercel env add SUPABASE_URL production
npx vercel env add SUPABASE_KEY production

# Redeploy with env vars
npx vercel --prod
```

You'll get: `https://circuitos-memory.vercel.app` (or similar)

**1.2 Setup Supabase Database**
- Go to https://supabase.com
- Create new project: "circuitos-memory"
- SQL Editor → Run `api/supabase-schema.sql`
- Settings → API → Copy service_role key

---

### Step 2: Update GHL Custom Fields (10 min)

Add these new custom fields in GHL:

**Existing Fields:**
- `lpr_score` (Number, 0-100)
- `fit_score` (Number, 0-40)
- `intent_score` (Number, 0-40)
- `timing_score` (Number, 0-20)

**NEW Fields for Memory:**
- `conversation_turns` (Number) - How many agent interactions
- `last_agent_action` (Text) - What agent did last
- `agent_confidence` (Text) - HIGH/MEDIUM/LOW
- `predicted_outcome` (Text) - What agent predicts will happen
- `actual_outcome` (Text) - What actually happened (for ML)
- `memory_enabled` (Checkbox) - Use agent memory for this contact

---

### Step 3: Create Enhanced GHL Workflow (30 min)

**Workflow Name:** "Virtual LPR → Multi-Agent Scoring & Outreach (Memory-Enhanced)"

#### TRIGGER: Contact Created or Tag Added
```
Trigger: Contact created
OR
Trigger: Tag added = "vlpr_signal_detected"
```

#### ACTION 1: Check if Memory Enabled
```
IF/ELSE Condition:
  IF custom_field.memory_enabled = TRUE
    → Go to ACTION 2 (Memory-Enhanced Scoring)
  ELSE
    → Go to OLD WORKFLOW (backward compatibility)
```

#### ACTION 2: Call Lead Scorer Agent (with Memory)
```
Webhook Action:
  URL: https://circuitos-memory.vercel.app/api/claude-agent-memory
  Method: POST
  Body:
  {
    "action": "score-lead",
    "contactId": "{{contact.id}}",
    "businessId": "{{business.id}}",
    "useMemory": true,
    "data": {
      "contact": {
        "first_name": "{{contact.first_name}}",
        "last_name": "{{contact.last_name}}",
        "email": "{{contact.email}}",
        "phone": "{{contact.phone}}",
        "city": "{{contact.city}}",
        "state": "{{contact.state}}",
        "custom_fields": {
          "distance_miles": "{{contact.custom_fields.distance_miles}}",
          "median_income": "{{contact.custom_fields.median_income}}",
          "neighborhood": "{{contact.custom_fields.neighborhood}}",
          "intent_signal": "{{contact.custom_fields.intent_signal}}",
          "vlpr_source": "{{contact.custom_fields.vlpr_source}}",
          "conversation_turns": "{{contact.custom_fields.conversation_turns}}"
        }
      },
      "business": {
        "name": "{{business.name}}",
        "category": "{{business.category}}",
        "location": "{{business.address}}"
      }
    }
  }

  Store Response:
    {{webhook_response.total_score}} → custom_fields.lpr_score
    {{webhook_response.breakdown.fit}} → custom_fields.fit_score
    {{webhook_response.breakdown.intent}} → custom_fields.intent_score
    {{webhook_response.breakdown.timing}} → custom_fields.timing_score
    {{webhook_response.next_action}} → custom_fields.last_agent_action
    {{webhook_response.confidence}} → custom_fields.agent_confidence
    {{webhook_response.metadata.conversationTurns}} → custom_fields.conversation_turns
```

#### ACTION 3: Dynamic Decision Based on Agent Intelligence
```
IF/ELSE Condition:
  IF webhook_response.next_action = "IMMEDIATE_COLD_EMAIL"
    AND custom_fields.conversation_turns = 1
    → Go to ACTION 4 (Generate Fresh Copy)

  ELSE IF webhook_response.next_action = "IMMEDIATE_COLD_EMAIL"
    AND custom_fields.conversation_turns > 1
    → Go to ACTION 5 (Escalate Messaging - agent knows this is follow-up)

  ELSE IF webhook_response.next_action = "NURTURE_SEQUENCE"
    → Go to Nurture Workflow

  ELSE IF webhook_response.next_action = "WAIT_FOR_MORE_DATA"
    → Add to "Need More Signals" campaign
```

#### ACTION 4: Call Copywriter Agent (First Touch)
```
Webhook Action:
  URL: https://circuitos-memory.vercel.app/api/claude-agent-memory
  Method: POST
  Body:
  {
    "action": "generate-copy",
    "contactId": "{{contact.id}}",
    "businessId": "{{business.id}}",
    "useMemory": true,
    "data": {
      "contact": {
        "first_name": "{{contact.first_name}}",
        "last_name": "{{contact.last_name}}",
        "city": "{{contact.city}}"
      },
      "channel": "EMAIL",
      "awareness_level": "Solution Aware",
      "business": {
        "name": "{{business.name}}",
        "category": "{{business.category}}"
      }
    }
  }

  Store Response:
    {{webhook_response.variants[0].subject}} → custom_fields.email_subject_a
    {{webhook_response.variants[0].body}} → custom_fields.email_body_a
    {{webhook_response.variants[0].cta}} → custom_fields.email_cta_a
```

**CRITICAL:** Copywriter agent will see Lead Scorer's analysis in conversation history automatically!

#### ACTION 5: Send Personalized Email
```
Email Action:
  To: {{contact.email}}
  From: {{business.email}}
  Subject: {{custom_fields.email_subject_a}}
  Body: {{custom_fields.email_body_a}}

  {{custom_fields.email_cta_a}}
```

#### ACTION 6: Wait for Response (24 hours)
```
Wait Action: 24 hours
```

#### ACTION 7: Check Engagement & Record Feedback
```
IF/ELSE Condition:
  IF contact.last_email_opened = YES
    OR contact.last_email_clicked = YES
    OR contact.replied = YES
    → Set custom_fields.actual_outcome = "HIGH_ENGAGEMENT"

  ELSE IF contact.booked_appointment = YES
    → Set custom_fields.actual_outcome = "CONVERTED"

  ELSE
    → Set custom_fields.actual_outcome = "NO_RESPONSE"

THEN:
  Webhook Action:
    URL: https://circuitos-memory.vercel.app/api/claude-agent-memory
    Method: POST
    Body:
    {
      "action": "record-feedback",
      "contactId": "{{contact.id}}",
      "businessId": "{{business.id}}",
      "data": {
        "conversationId": "conv-{{contact.id}}",
        "predicted": "{{custom_fields.predicted_outcome}}",
        "actual": "{{custom_fields.actual_outcome}}",
        "learnedPatterns": {
          "signals": ["{{custom_fields.intent_signal}}", "PROXIMITY_{{custom_fields.distance_miles}}MI"],
          "worked": "{{contact.booked_appointment}}",
          "industry": "{{business.category}}",
          "copy_variant": "A"
        }
      }
    }
```

**This feedback trains the ML model to improve future Virtual LPR scoring!**

---

## 🤖 PART 3: How Claude Code Can Dictate Workflows

### Using Me (Claude Code) to Auto-Generate Workflows

Just like HighLevel's Workflow AI, I can **analyze your Virtual LPR logic and generate complete GHL workflows** for you.

#### Example: Tell Me What You Want

**Your Request:**
> "Claude, create a workflow for leads with LPR score 85+ who came from GMB directions clicks. They should get an immediate SMS, then if they reply, book them into a call. If no reply in 2 hours, send an email. Track everything in the pipeline."

**I Generate:**
```json
{
  "workflow_name": "High-Intent GMB Leads → Omnichannel Sequence",
  "trigger": {
    "type": "custom_field_updated",
    "field": "lpr_score",
    "condition": "greater_than_or_equal",
    "value": 85
  },
  "filter": {
    "intent_signal": "GMB_DIRECTIONS_CLICKED"
  },
  "actions": [
    {
      "type": "sms",
      "delay": "immediate",
      "content": "Hi {{contact.first_name}}! Saw you looked up directions to {{business.name}}. We're 5min away at {{business.address}}. Want to book a free session? Reply YES and I'll call you in the next hour. -{{business.owner_name}}"
    },
    {
      "type": "wait_for_engagement",
      "duration": "2 hours",
      "engagement_type": "sms_reply"
    },
    {
      "type": "if_else",
      "condition": "contact.replied_to_sms = YES",
      "true_branch": [
        {
          "type": "tag",
          "tag_name": "HOT - Replied to SMS"
        },
        {
          "type": "book_calendar",
          "calendar_id": "{{business.sales_calendar}}",
          "send_booking_link": true
        }
      ],
      "false_branch": [
        {
          "type": "email",
          "subject": "Quick question about {{business.name}}",
          "body": "{{custom_fields.email_body_a}}"
        },
        {
          "type": "add_to_pipeline",
          "pipeline": "Virtual LPR - High Intent",
          "stage": "Contacted - No Response"
        }
      ]
    }
  ]
}
```

I can generate this in GHL-compatible JSON or as step-by-step instructions!

---

## 🎯 PRACTICAL EXAMPLES

### Example 1: "Adaptive Follow-Up Based on Engagement"

**Your Request:**
> "If a lead has been contacted 3+ times (conversation_turns >= 3) but never responded, switch to a passive nurture campaign instead of more cold emails."

**I Generate:**
```
Workflow: "Adaptive Follow-Up - Memory-Aware"

TRIGGER: Contact tag added = "ready_for_follow_up"

ACTION 1: Get conversation summary
  Webhook: GET https://circuitos-memory.vercel.app/api/claude-agent-memory
  Body: {"action": "get-summary", "contactId": "{{contact.id}}"}

ACTION 2: Dynamic routing based on conversation history
  IF webhook_response.turnCount >= 3
    AND custom_fields.actual_outcome = "NO_RESPONSE"
    → Move to "Passive Nurture" campaign (LinkedIn content, retargeting ads)

  ELSE IF webhook_response.turnCount < 3
    → Continue aggressive outreach (call Copywriter agent for fresh angle)
```

### Example 2: "Virtual LPR Score-Based Channel Selection"

**Your Request:**
> "For leads with LPR score 90+, call them directly. For 70-89, email first. For 50-69, nurture with content. Let the agent decide the best channel based on intent signals."

**I Generate:**
```
Workflow: "Intelligent Channel Router"

TRIGGER: Contact scored (lpr_score updated)

ACTION 1: Call Lead Scorer Agent (with memory)
  [Already detailed above]

ACTION 2: Agent-driven channel selection
  IF webhook_response.total_score >= 90
    AND webhook_response.detailed_attribution.intent_reasons includes "PHONE_CALL"
    → Create task for sales rep: "CALL NOW - Hot lead from {{custom_fields.vlpr_source}}"

  ELSE IF webhook_response.total_score >= 70
    AND webhook_response.next_action = "IMMEDIATE_COLD_EMAIL"
    → Call Copywriter Agent → Send Email

  ELSE IF webhook_response.total_score >= 50
    → Add to "Educational Content" drip campaign

  ELSE
    → Tag "Low Quality - Monitor"
```

### Example 3: "Multi-Touch Attribution with ML Feedback"

**Your Request:**
> "Track every Virtual LPR signal (GMB view, website visit, email open) and let the agent learn which sequence of signals leads to conversions."

**I Generate:**
```
Workflow: "Signal Tracking + ML Learning"

TRIGGER: Any Virtual LPR signal detected (webhook from MCP servers)

ACTION 1: Log signal to conversation history
  Webhook: POST /api/claude-agent-memory
  Body: {
    "action": "score-lead",
    "contactId": "{{contact.id}}",
    "useMemory": true,
    "data": {
      "contact": {...},
      "new_signal": "{{trigger.signal_type}}"
    }
  }

ACTION 2: Agent analyzes signal in context
  - Agent sees: "This is the 3rd GMB view in 7 days"
  - Agent decides: "High intent - escalate to immediate outreach"

ACTION 3: Record outcome later
  Wait: 48 hours
  Webhook: record-feedback
  - Did they convert after seeing 3 GMB views?
  - System learns: "3 GMB views in 7 days = 78% conversion rate"
  - Adjust Virtual LPR intent weight: GMB views +5 points
```

---

## 🔄 PUTTING IT ALL TOGETHER

### Your Complete Integration Flow:

```
┌─────────────────────────────────────────────────────────┐
│ Virtual LPR Signals (MCP Servers)                       │
│ - Google Analytics (website visit)                      │
│ - GMB (directions click, call, listing view)            │
│ - Census data (demographics)                            │
│ - OpenStreetMap (proximity)                             │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ GHL Trigger: New Contact or VLR Signal Detected         │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ Claude Agent Memory API - Lead Scorer                   │
│ - Analyzes all signals in context                       │
│ - Remembers past scoring for this contact               │
│ - Returns: score + next action + confidence             │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ GHL Dynamic Workflow Decision                           │
│ - Agent dictates: Email? SMS? Call? Wait?               │
│ - Based on: score + conversation history + signals      │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ Claude Agent Memory API - Copywriter                    │
│ - Sees Lead Scorer's analysis (shared memory!)          │
│ - Generates personalized copy                           │
│ - Knows if this is 1st touch or follow-up               │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ GHL Executes: Email/SMS/Call                            │
│ - Uses agent-generated copy                             │
│ - Tracks engagement                                     │
└──────────────────┬──────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────┐
│ ML Feedback Loop (24-48 hours later)                    │
│ - Did they convert?                                     │
│ - Record: predicted vs actual outcome                   │
│ - System learns: GMB directions + 0.8mi = 82% convert   │
│ - Auto-adjust Virtual LPR weights                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 HOW TO USE ME (CLAUDE CODE) TO GENERATE WORKFLOWS

### Option 1: Natural Language Requests

Just tell me what you want, and I'll generate the complete GHL workflow JSON:

**Examples:**
- "Create a workflow for fitness leads with LPR score 75+ from GMB"
- "Build a follow-up sequence for leads who opened but didn't reply"
- "Make an escalation workflow if no response after 3 touches"

### Option 2: Virtual LPR Logic-Based Auto-Generation

I can analyze your Virtual LPR scoring logic and automatically create workflows:

**You provide:**
- Virtual LPR score breakdown (fit, intent, timing)
- Available signals (GMB, GA4, demographics)
- Business goals (book appointments, sell memberships, etc.)

**I generate:**
- Complete multi-step workflows
- Agent collaboration sequences
- ML feedback integration
- Dynamic decision trees

### Option 3: Iterative Refinement

Start with a basic workflow, then refine:

**Turn 1:** "Create email sequence for high-intent leads"
**Turn 2:** "Add SMS if no email response in 24hrs"
**Turn 3:** "If they reply via SMS, book them on calendar automatically"

I'll update the workflow each time!

---

## 📊 COMPARISON: Before vs After Integration

### BEFORE (Static GHL Workflows):
- ❌ One-size-fits-all rules
- ❌ No memory of past interactions
- ❌ Manual workflow building
- ❌ No learning from outcomes

### AFTER (Memory-Enhanced + Claude Code):
- ✅ Dynamic decisions based on conversation history
- ✅ Agents remember and collaborate
- ✅ I auto-generate workflows from your Virtual LPR logic
- ✅ Self-improving ML loop

---

## 🚀 NEXT STEPS

1. **Deploy Memory API** (30 min) - Follow api/DEPLOYMENT-GUIDE.md
2. **Update GHL Custom Fields** (10 min) - Add conversation tracking fields
3. **Create First Memory-Enhanced Workflow** (30 min) - Use template above
4. **Test with Sample Lead** (10 min) - Verify agents collaborate correctly
5. **Ask Me to Generate More Workflows** - Just describe what you need!

---

**Ready to start?** Let me know which Virtual LPR scenario you want to automate first, and I'll generate the complete workflow for you!

**© 2025 Circuit OS™**
**Powered by Claude Agent Memory + Virtual LPR Intelligence**
