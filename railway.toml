[build]
builder = "DOCKERFILE"
dockerfilePath = "docker-compose.yml"

[deploy]
startCommand = "echo 'Railway deployment via docker-compose'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
