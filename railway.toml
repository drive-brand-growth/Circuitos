[build]
builder = "NIXPACKS"
buildCommand = "echo 'Using Nixpacks builder for Railway deployment'"

[deploy]
startCommand = "echo 'Railway deployment ready'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway doesn't support docker-compose volumes
# We'll deploy individual services instead
